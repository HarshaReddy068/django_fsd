create templates folder in app3 and 
create mytemplate.html file
and write the CODE

----------------------------------------------------------------

In settings.py, in inst_app 
 
INSTALLED_APPS = [
    "django.contrib.admin",
    "django.contrib.auth",
    "django.contrib.contenttypes",
    "django.contrib.sessions",
    "django.contrib.messages",
    "django.contrib.staticfiles",
    "app3",                             ## Add this
]

--------------------------------------------------------------------

